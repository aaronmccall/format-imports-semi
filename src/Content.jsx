import React from 'react'

const Component = () => (
  <p style={{ width: '90vw', height: '60vh' }}>
    If you run <code><pre>yarn sort-imports</pre></code>, semi-colons will be added to the import statements.
    If you run <code><pre>yarn ling</pre></code>, semi-colons will be removed from the import statements.
  </p>
)

export default Component
